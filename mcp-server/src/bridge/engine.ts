/**
 * Engine bridge — reserved for future integration with the full AI engine.
 * Currently unused since the MCP server focuses on data access and analysis,
 * while the agent (Claude Code) does the actual writing.
 */

export { getDb } from './db.js';

